# Name,   Type, SubType, Offset,   Size,     Flags
# Same offsets/sizes as the ESP-IDF stock 4MB default.csv table. The last
# partition backs LittleFS preset storage (see src/Components/persistence.h)
# -- it MUST be named "spiffs": the Arduino-ESP32 LittleFS library looks up
# its partition by that label, not by type/subtype, so any other name makes
# LittleFS.begin() fail with "partition \"spiffs\" could not be found" even
# though the partition itself exists. app0/app1 OTA slots are untouched.
nvs,      data, nvs,     0x9000,  0x5000,
otadata,  data, ota,     0xe000,  0x2000,
app0,     app,  ota_0,   0x10000, 0x140000,
app1,     app,  ota_1,   0x150000,0x140000,
spiffs,   data, spiffs,  0x290000,0x160000,
